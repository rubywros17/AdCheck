spring:
  datasource:
    url: jdbc:h2:mem:adcheck;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  # Public test fixture only. Production always uses JWT_SECRET.
  secret: QUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUE=
  expiration: 3600000
